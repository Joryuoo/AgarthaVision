# 2.2 Human-in-the-Loop Validation

```mermaid
classDiagram
direction LR

class VerificationSheet
class ManualSheet

class VerificationViewModel {
  +onToggleRepeat()
  +onSubmit()
}

class ManualCaptureViewModel {
  +onSubmit()
}

class FlaggedFrameStore {
  +toggleRepeat(frame)
  +remove(frame)
}

class SubmitVerificationUseCase {
  <<usecase>>
  +invoke(frame, answers, missedEgg, userNote, isRepeat)
}

class SubmitManualCaptureUseCase {
  <<usecase>>
  +invoke(frame, species, otherSpeciesText, userNote, isRepeat)
}

class AuthRepository {
  +getCurrentUserId(): String
}

class LocationProvider {
  +getCurrentLocation()
}

class SampleDao {
  +updateSampleOnVerify(...)
}

class DetectionDao {
  +insertDetections(...)
  +insertDetection(...)
}

class SyncSampleUseCase {
  +invoke(sampleId)
}

VerificationSheet --> VerificationViewModel : events
ManualSheet --> ManualCaptureViewModel : events
VerificationViewModel --> SubmitVerificationUseCase : calls
ManualCaptureViewModel --> SubmitManualCaptureUseCase : calls
SubmitVerificationUseCase --> AuthRepository
SubmitVerificationUseCase --> LocationProvider
SubmitVerificationUseCase --> SampleDao
SubmitVerificationUseCase --> DetectionDao
SubmitVerificationUseCase --> SyncSampleUseCase
SubmitVerificationUseCase --> FlaggedFrameStore : remove()/toggleRepeat()

SubmitManualCaptureUseCase --> AuthRepository
SubmitManualCaptureUseCase --> LocationProvider
SubmitManualCaptureUseCase --> SampleDao
SubmitManualCaptureUseCase --> DetectionDao
SubmitManualCaptureUseCase --> SyncSampleUseCase
```
