# 1.1 Live Image Acquisition

```mermaid
classDiagram
direction LR

class CameraManager {
  +bindAnalysis(lifecycleOwner, previewView, analyzer, analyzerExecutor) Camera
}

class ImageAnalysisAnalyzer <<interface>>
class FrameSampler {
  +latestFrameBytes: StateFlow
  +analyze(image)
}

ImageAnalysisAnalyzer <|.. FrameSampler

class SessionManager
class SessionState
class InferFrameUseCase

FrameSampler --> SessionManager
FrameSampler --> InferFrameUseCase
SessionManager --> SessionState

class MicroscopyViewport <<Composable>>
MicroscopyViewport --> CameraManager
MicroscopyViewport --> ImageAnalysisAnalyzer : analyzer

class CaptureViewModel <<ViewModel>>
CaptureViewModel --> SessionManager
CaptureViewModel --> FrameSampler
CaptureViewModel --> FlaggedFrameStore

class FlaggedFrameStore
```
