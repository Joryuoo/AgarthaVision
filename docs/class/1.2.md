# 1.2 Image Payload Transmission

```mermaid
classDiagram
direction LR

class FrameSampler {
  +dispatchInference(sessionId, jpegBytes)
}

class InferFrameUseCase {
  <<usecase>>
  +invoke(sessionId, jpegBytes)
}

class InferenceApi {
  <<retrofit>>
  +infer(jpegBytes): InferenceResponseDto
}

class InferenceResponseDto

class FlaggedFrameStore {
  +state: StateFlow
  +add(frame)
  +remove(frame)
}

class PersistFlaggedFrameUseCase {
  <<usecase>>
  +invoke(frame)
}

class SampleImageStore {
  +persistJpeg(userId, sampleId, bytes): String
}

class DeviceIdProvider {
  +id: String
}

class AuthRepository {
  +getCurrentUserId(): String
}

class SampleDao {
  <<room>>
  +insertSample(entity)
  +updateSampleOnVerify(...)
}

class DetectionDao {
  <<room>>
  +insertDetections(...)
}

class SyncSampleUseCase {
  <<usecase>>
  +invoke(sampleId)
}

class SampleRemoteDataSource {
  +syncSample(sample, detections, imageBytes)
}

FrameSampler --> InferFrameUseCase : calls
InferFrameUseCase --> InferenceApi : POST /infer
InferFrameUseCase --> FlaggedFrameStore : add()
FlaggedFrameStore --> PersistFlaggedFrameUseCase : triggers
PersistFlaggedFrameUseCase --> AuthRepository : userId
PersistFlaggedFrameUseCase --> SampleImageStore : persistJpeg()
PersistFlaggedFrameUseCase --> DeviceIdProvider : deviceId
PersistFlaggedFrameUseCase --> SampleDao : insertSample()

SyncSampleUseCase --> SampleDao : read/update
SyncSampleUseCase --> DetectionDao : read
SyncSampleUseCase --> SampleRemoteDataSource : upload/insert
```
