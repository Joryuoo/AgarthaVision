# 3.2 Administrative Aggregated Report Generation

```mermaid
classDiagram
direction LR

class ReportType <<enumeration>> {
  SESSION
}

note for ReportType "Only SESSION reports are implemented today; administrative aggregation is deferred."

class ReportRepository <<interface>> {
  +observeForSession(sessionId, userId) Flow
  +getReportsPendingSync(userId) List
}

class LocalReportRepository
ReportRepository <|.. LocalReportRepository
LocalReportRepository --> ReportDao

class SyncReportUseCase <<UseCase>>
SyncReportUseCase --> ReportDao
SyncReportUseCase --> ReportRemoteDataSource

class ReportRemoteDataSource
ReportRemoteDataSource --> SupabaseClient

class ReportDao <<DAO>>
class SupabaseClient
class Report
```
