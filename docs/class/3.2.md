# 3.2 Administrative Aggregated Report Generation

```mermaid
classDiagram
direction LR

class AdministrativeAggregateReportUseCase {
  <<deferred>>
  +generateAggregatedReport(dateRange, filters)
}

class ReportType {
  <<enum>>
  SESSION
}

class ReportRepository {
  +getReportsPendingSync(userId)
}

class SyncReportUseCase {
  +invoke(reportId)
}

note for AdministrativeAggregateReportUseCase "Administrative aggregated reports are deferred.\nCurrent codebase only supports per-session reports."

ReportRepository --> SyncReportUseCase : sync pending reports
```
