package com.agarthavision.ui.login

import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.wrapContentSize
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Placeholder for the real Login screen.
 *
 * Owner: Beansman. Implementation track: Phase 2 §A in the active plan
 * (LoginScreen + LoginViewModel against [com.agarthavision.core.di.SupabaseModule]).
 */
@Composable
fun LoginScreenPlaceholder() {
    Text(
        text = "Login — TODO",
        modifier = Modifier.fillMaxSize().wrapContentSize(),
    )
}
