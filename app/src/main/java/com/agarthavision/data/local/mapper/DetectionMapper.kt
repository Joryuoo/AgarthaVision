package com.agarthavision.data.local.mapper

import com.agarthavision.data.local.entity.DetectionEntity

/**
 * Mappers for `DetectionEntity` will land with the `Detection` domain model and
 * the Roboflow/Inference DTO mapper (Sprint 1 — jojseph). This file is a placeholder
 * so the package exists and references compile.
 */
@Suppress("unused")
private val DetectionMapperPackageMarker: DetectionEntity? = null
