package com.agarthavision.domain.model

/**
 * User-selected UI theme mode, persisted on-device. Defaults to [LIGHT]
 * (the clinical calibration); Capture stays dark regardless of mode.
 */
enum class ThemeMode {
    LIGHT,
    DARK,
}
